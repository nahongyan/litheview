name: Validate public SDK repository

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  repository:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate repository contents
        shell: pwsh
        run: ./scripts/verify-public-repo.ps1
